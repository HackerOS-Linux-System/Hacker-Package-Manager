# Hacker Package Manager
HPM is community repo for HackerOS.

## Commands:
- hpm install (package)
- hpm install (package)
- hpm build (package name)
- hpm search (package name)
- hpm run (package) (version - optional)
