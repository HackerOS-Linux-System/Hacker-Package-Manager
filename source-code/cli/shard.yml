name: hpm
version: 0.4

description: |
  HackerOS Package Manager (HPM) – CLI package manager for HackerOS,
  written in Crystal.

authors:
  - HackerOS Team <hackeros@localhost>

license: GPL-3.0

targets:
  hpm:
    main: src/main.cr

crystal: ">= 1.8.0"

# No external dependencies required
dependencies:
